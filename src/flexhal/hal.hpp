\
#pragma once

// Include headers for HAL modules
#include "hal/gpio.hpp"

// Potentially other HAL modules like uart, spi, i2c, etc. will be included here in the future.

namespace flexhal {
/**
 * @brief Hardware Abstraction Layer (HAL)
 * 
 * This namespace provides interfaces and implementations for interacting with
 * common microcontroller peripherals.
 */
namespace hal {

// No specific definitions needed directly in this header, 
// it primarily serves to include the module headers.

} // namespace hal
} // namespace flexhal

#pragma once

// Include HAL module headers for Arduino
#include "hal/gpio.hpp" // Include GPIO HAL module

// If other HAL modules for Arduino (like SPI, I2C) are added later,
// include their respective headers (e.g., "hal/spi.hpp") here.
